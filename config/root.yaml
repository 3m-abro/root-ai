version: 1

system:
  name: ROOT
  role: chief_of_staff
  environment: development

principles:
  user_is_final_authority: true
  least_privilege: true
  reversible_by_default: true
  explicit_approval_for_sensitive_actions: true
  one_next_action: true

attention:
  default_task_limit: 1
  max_active_projects: 3

  energy_states:
    - HIGH
    - NORMAL
    - LOW
    - SCATTERED

  interrupt_policy:
    URGENT: interrupt
    IMPORTANT: notify
    NORMAL: queue
    LOW: silent

tasks:
  states:
    - HUMAN
    - AI
    - AUTOMATION
    - DELEGATED
    - WAITING
    - PARKED
    - BLOCKED
    - COMPLETED

  new_ideas_default_state: PARKED

approvals:
  level_0:
    name: observe
    approval_required: false

  level_1:
    name: safe_reversible
    approval_required: false

  level_2:
    name: sensitive_external_action
    approval_required: true

  level_3:
    name: human_only
    approval_required: true

execution:
  environments:
    vps:
      enabled: true
      purpose:
        - root
        - hermes
        - automation
        - orchestration

    local_pc:
      enabled: true
      purpose:
        - coding
        - docker
        - local_tools
        - desktop_execution

  unrestricted_remote_shell: false
  unrestricted_sudo: false

sources_of_truth:
  projects: leantime
  automation: n8n
  code: git
  agent_runtime: hermes
  runtime_state: hermes
  secrets: environment

runtime:
  hermes_profile: chief
  dashboard:
    bind: 127.0.0.1
    port: 9119
    exposure: tailscale_serve

workers:
  profiles:
    - research
    - engineering
    - marketing
    - operations

  dynamic_specialists: true
  permanent_specialist_agents: false

integrations:
  leantime:
    enabled: true
    access: n8n_rpc
    contract: config/integrations/leantime.yaml
    path: ROOT/Hermes → n8n → Leantime
    credentials:
      read_write_separated: true
      source: environment

    n8n:
      enabled: true
      role: automation_and_leantime_gateway
      leantime_rpc:
        workflow: ROOT - Leantime RPC
        webhook_path: /webhook/root/leantime/rpc
        auth_header: x-root-api-key
        allowlist_only: true
        reject_arbitrary_rpc: true

  github:
    enabled: true

  pc_agent:
    enabled: false

  voice:
    enabled: false